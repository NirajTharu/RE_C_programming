// Take a number(n) from user and ouput its cube
#include<stdio.h>

int main()
{
    int n;
    printf("Enter the value of the n for the claculation of the cube:");
    scanf("%d",&n);
    printf("The cube of the given number is:%d\n",n*n*n);
    return 0;
}